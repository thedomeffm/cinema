{% extends 'layout.html.twig' %}

{% block content %}
    <h1>Movie</h1>

    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button type="submit" class="btn btn-success">Speichern</button>
    {{ form_end(form) }}
{% endblock %}