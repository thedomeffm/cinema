{% extends 'layout.html.twig' %}

{% block crumbs %}
    <ol class="breadcrumb">
        <li><a href="{{ path('admin_index') }}">Administration</a></li>
        <li class="active">Säle</li>
    </ol>
{% endblock %}

{% block content %}
    <div class="page-header">
        <h1>Säle <small class="pull-right"><a class="btn btn-success btn-block" href="{{ path('hall_create') }}">+ Erstellen</a></small></h1>
    </div>

    {% if halls is not empty %}
        {#
            <ul class="list-group">
                {% for hall in halls %}
                    <li class="list-group-item">{{ hall.name }}</li>
                {% endfor %}
            </ul>
        #}
        <table class="table">
            <thead>
                <tr>
                    <th>Name</th>
                </tr>
            </thead>
            <tbody>
                {% for hall in halls %}
                    <tr>
                        <td>{{ hall.name }}</td>
                        <td class=""><a href="{{ path('hall_remove', {'id': hall.id}) }}"><span class="glyphicon glyphicon glyphicon-remove"></span></a></td>
                    </tr>
                {% endfor %}
            </tbody>
        </table>


    {% else %}
        <b>Keine Sääle eingetragen</b>
    {% endif %}
{% endblock %}